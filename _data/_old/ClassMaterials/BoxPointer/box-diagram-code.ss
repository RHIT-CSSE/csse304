(define v (cons 'a 'b))
(define w (list 'a 'b))
(define x '((1 2) 3 (4 5)))
(define y (cons (car x) (cdr x)))
(define z (cons (cdr x ) x))

(write v) (newline)
(write w) (newline)
(write x) (newline)
(write y) (newline)
(write z) (newline)


