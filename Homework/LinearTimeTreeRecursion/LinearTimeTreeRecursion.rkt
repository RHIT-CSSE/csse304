#lang racket

(provide vector-append-list group-by-two group-by-n bt-leaf-sum bt-inorder-list bt-max bt-max-interior)

(define vector-append-list
  (lambda (a b)
    (nyi)))

(define group-by-two
  (lambda (a)
    (nyi)))

(define group-by-n
  (lambda (a b)
    (nyi)))

(define bt-leaf-sum
  (lambda (a)
    (nyi)))

(define bt-inorder-list
  (lambda (a)
    (nyi)))

(define bt-max
  (lambda (a)
    (nyi)))

(define bt-max-interior
  (lambda (a)
    (nyi)))

;;--------  Used by the testing mechanism   ------------------

(define-syntax nyi
  (syntax-rules ()
    ([_]
     [error "nyi"])))
